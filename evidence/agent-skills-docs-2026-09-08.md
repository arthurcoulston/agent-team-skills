---
id: agent-skills-docs-2026-09-08
kind: source_reading
source_title: Agent Skills documentation
source_url: https://code.claude.com/docs/en/skills
source_date: unknown
read_on: 2026-09-08
read_by: mason
method: automated fetch, page converted to markdown
---

# Reading record — Agent Skills documentation, 2026-09-08

What the page said about the `description` field on 2026-09-08. Cited by
[knowledge/skill-entrypoint-description.md](../knowledge/skill-entrypoint-description.md).

## Quoted

> What the skill does and when to use it. Claude uses this to decide when to
> apply the skill. If omitted, uses the first paragraph of markdown content.
> Put the key use case first: the combined `description` and `when_to_use`
> text is truncated at 1,536 characters in the skill listing to reduce
> context usage.

And, on whether the field is required:

> Recommended — Claude uses this to decide when to invoke the skill.

## No date on the source

The page carries no publication date, no "last updated" date and no version
number. This is why the citing entry records `source_date: unknown` rather
than a guess. It also means the reading date is the only date that bounds
this record: the page may have changed since, and a later check needs its own
record rather than an edit to this one.

## How it was read

Fetched and converted to markdown by an automated tool, then quoted from that
conversion — not read from the rendered page in a browser. The quotes above
are therefore faithful to what the tool returned. Anything turning on exact
punctuation, or on material the conversion may have dropped (tables, code
samples, collapsed sections), needs a direct read before it is relied on.

## What it does not establish

It records what the documentation asserts. It does not show that a
description written this way actually gets a skill loaded more often — that
is a behavioural question, and this record is not evidence for it.
